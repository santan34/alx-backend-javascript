/**
 * this functions console.logs
 * @param {string} msg - the message to print
 * @return {void} - doesnt return anything
 */

const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
